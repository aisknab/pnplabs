# Compatible-support slack correction publication

## Why publish now

The core correction refutes the unrestricted raw-port-completeness reading of
the manuscript's compatible-support slack law. It does not revoke the checked
acyclic framed or restricted replacement laws, establish the complete saturated
route, or decide P versus NP. This is a major claim-boundary correction, not a
new positive milestone. The general source-derived admissibility and first-loss
routing obligation remains the mathematical priority.

Publish one coherent release after the correction's core PR is merged and its
required exact-merge verification is green. Batch the pending earned M265-M280
rows from that same source. Do not invent M281, award checkpoint credit for the
counterexample, or mix an unpublished source pin with the existing public state.

## Source and expectation matrix

| Producer or boundary | Consumers to reconcile before tests | Focused evidence |
| --- | --- | --- |
| Update schema v4, with separate corrections | Existing milestone generator, update tests, HTML and Atom output | Exact schema, identity, source, history, escaping, ordering and no-credit mutations |
| Correction evidence | Canonical status non-claim, compiled declaration metadata, source-bound audit | Current and historical source binding; changed, missing or axiom-backed evidence rejected |
| Exact merged core snapshot | Sync generator, frozen batch contracts, index, manifest, browser descriptor, audit targets | Exact reviewed fields, theorem sets, axiom closures, source hashes and mirrored bytes |
| Current project explanation | Homepage bottom line, status, FAQ, paper, architecture, README and current reviewer documentation | Conservative correction scope, complete pending ledger, separate canonical metrics |
| Recorded milestone boundaries | Update technical details, status-ledger context, progress and update generators | Explicit historical scope, byte-preserved recorded limitations and unchanged update objects |
| Generated public bytes | Updates/feed/graphic, downloads, release seal, browser checks and deployment provenance | Deterministic check mode, source-bound site tests, negative cases and production bytes |

Keep every existing update object and pinned historical artefact unchanged.
Label recorded milestone limitations as historical scope, not the current
project status. An old reviewed boundary can correctly say an obligation was
open when earned even after a later milestone closes it. Preserve those exact
source-bound statements and link readers to the current tracker; revise only
site-owned current summaries that contradict the canonical current state.
The versioned update model keeps earned entries and correction notices separate.
Only earned entries contribute to completeness and ordinal counts. Corrections
carry a canonical historical progress snapshot and exact source evidence; they
cannot add earned rows, points, a milestone identifier, or an earned ordinal.

The reviewed correction contract fixes the limitation and all eighteen theorem
metadata records independently of generated status. Either canonical finding
marker requires a separately labelled notice; later updates must not silently
drop or weaken it. These publication checks do not replace the core theorem,
type, source-closure or axiom audit. The existing exact-source binding and sealed
inventory checks remain the site boundary, with no second Lean build.

The synthetic cross-repository fixture keeps current notices bound to its
current test commit and historical notices bound to separate retained test
objects. Inventory rewrites reseal the dependent status before validation. These
fixtures exercise publication provenance, never mathematical proof authority.

The generator validates current correction evidence against the current status
and compiled inventory. A correction referencing an older published snapshot
must also pass the source-bound audit against that exact historical core commit.
Preserve that evidence when later publications advance; never rewrite a historic
correction into the new source coordinate.

## Implementation order

1. Prepare the correction-aware update adapter and its positive/hostile tests
   without changing the published core pin. Use synthetic correction fixtures;
   do not publish the actual finding against the old snapshot.
2. Wait for every normal core PR check, confirm the exact unchanged head, merge
   manually, fetch the actual merge and require its normal post-merge checks.
3. Reproduce the exact core merge identity and source seal. Reuse unchanged proof
   evidence under the existing tree/input/toolchain/boundary policy.
4. Synchronize all canonical core artefacts and the pending earned rows from that
   exact merge. Freeze the batch and correction claim/evidence contracts; add the
   separately labelled correction update using the actual publication timestamp.
5. Reconcile every active publication surface and every affected expectation
   before targeted checks. Derive values from canonical payloads, retain hostile
   invariants and historical records, and omit PDF page totals from reader copy.
6. Run the required source-bound site and negative checks, normal PR checks and
   exact-head reproduction. PNPLabs must not run Lean or repeat the core suite.
7. Merge, verify the exact site merge, deploy through the authorized narrow
   interface, independently verify production, then complete the requested
   repository backups. Return to the consequential general mathematical target.

## Current publication boundary

The core correction is merged and verified at commit `f14cb7a87004ebedfa55351f6ba20d68a333cc18` (tree `1cbaf64d8a7303b154848ec73f38e85e406defca`). Its normal PR and
post-merge checks and independent exact-merge reproduction have passed. The
website now consumes that exact canonical source; the deployed site remains
unchanged until the separate publication and deployment gates pass.

The prepared release batches M265-M280 in canonical history order and includes
one separately labelled correction notice. All earlier earned update objects
remain unchanged. Every row retains its original review coordinate and progress
snapshot; the shared publication timestamp belongs to this verified source batch.

Formal artefact coverage: 256 of 258 current scoped publication rows earned.
Risk-weighted proof completion estimate: 40%. Uncertainty range: 20% to 40%.
Global gates closed: 0 of 5. No fixed checkpoint changes in this publication.
The correction adds no positive milestone or proof-completion credit.

Focused source-binding, current-copy, history, update/feed, release-seal, link
and report-integrity checks have passed. Remaining gates are the complete
source-bound website and hostile audit, the normal site PR and post-merge
checks, exact-site-merge reproduction, deployment, independent production
verification and the requested repository backups. No site check recompiles
the already verified core Lean source.

The next mathematical target remains complete source-derived admissibility and
successful global routing. Supporting lemmas belong inside that general
obligation, not in a sequence of easier standalone milestones.
