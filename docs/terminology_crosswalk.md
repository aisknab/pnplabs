# Terminology Crosswalk

> **Current status:** Formal reconstruction is in progress. The repository does not currently
> establish `P = NP`, and no historical checker release is current theorem authority. See the
> authoritative [`../public/pnp-status.json`](../public/pnp-status.json), upstream
> [`status/FORMAL_RECONSTRUCTION_STATUS.json`](https://github.com/aisknab/pnp/blob/e355f1c93ef17f7d8069cc128b204a351b4792b7/status/FORMAL_RECONSTRUCTION_STATUS.json),
> and [`docs/FORMAL_RECONSTRUCTION.md`](https://github.com/aisknab/pnp/blob/e355f1c93ef17f7d8069cc128b204a351b4792b7/docs/FORMAL_RECONSTRUCTION.md).

This crosswalk translates current formal-reconstruction terms and historical assertion-checker terms
into more standard complexity-theory, proof-engineering, and formal-methods vocabulary. The current
canonical download is the 142-page inventory-derived report. Every bare `Report` citation in the
historical tables below refers only to the 57-page manuscript at source tag
`final-pnp-proof-report-hardened-7072f8d`, commit
`7072f8d0bda6d44d240f9bb3fad624fd357e1278`; it never refers to the current canonical aliases.
Anything not directly executable from this checkout is marked with an evidence-status label rather
than inferred.

Evidence-status labels mean:

- **Confirmed in public checkout**: this checkout contains the enforcing public file or local fixture code and a local command can check it.
- **Defined in pinned source; independent source audit required**: the term has a pinned source/checker coordinate under `sourceRef`; the implementation still needs independent inspection.
- **Present only in the historical report; formal source evidence required**: the pinned 7072f8d manuscript defines or uses the term, but that prose is not current formal evidence.
- **Generated artefact required**: the term depends on generated proof-report artefacts pinned under `artifactRef`.
- **Missing or ambiguous; author clarification requested**: pinned source, release docs, and generated artefacts do not resolve the definition. Author clarification can resolve ambiguity but is not verification evidence.
- **Independent mathematical review required**: the term or implication remains a mathematical-review target even if implementation coordinates are valid.

Checker paths have two different statuses:

- **Local fixture checker**: code in [tools/reviewer-fixture-checker.mjs](../tools/reviewer-fixture-checker.mjs). It checks only tiny JSON fixtures in [examples/minimal/](../examples/minimal/). It does not check the theorem, the real generated package, or the source/checker implementation named by the report.
- **Historical report-stated checker**: a checker named by the pinned 7072f8d manuscript. The
  current canonical report does not adopt those checker assertions. The mapped source paths are
  historical audit targets rather than current theorem evidence.
- **Pinned cross-repo target**: a path assigned to `sourceRef`, `docsRef`, or `artifactRef` in [source_checker_map.md](source_checker_map.md) and [audit_targets.json](audit_targets.json). Running source/checker tests checks the package according to its implementation; it is not external mathematical acceptance.

## Current Formal-Evidence Terms

| Term | Meaning | Current status |
| --- | --- | --- |
| Compiled theorem inventory | Canonical list of public Lean declaration names, modules, kinds, and collected axiom closures, plus raw kernel types for 3,140 reviewed milestone candidates | 31,198 declarations, 15,936 theorem-kind, 7,703 assumption-free theorem-kind, 320 modules, no project-specific axioms; 15,181 private compiler auxiliaries are excluded |
| Reviewed milestone fingerprint | Domain-separated SHA-256 of an exact theorem name and raw kernel type, combined with approved axiom closure and complete source-closure identity | 3,140 theorem candidates support 176 of 178 scoped milestone rows through the established concrete, reduction, residual, Packet/HB, sidecar, compatibility, PCCPack, proof-bearing PCCMin, same-candidate BCEL, canonical grouping, raw cut-ledger, sparse V53 constant-cut-basis, and sparse activation-route results. No earned closure contains a project axiom. |
| Strict-v0 locked-NAND encoded semantic boundary | Canonical token, normalized-circuit, and complete-instance codecs plus a pure all-bitstring construction | Valid normalized circuits and complete instances round-trip exactly; malformed source bytes fail closed and valid source bytes preserve the locked-NAND semantic threshold. This is not itself a machine; the following source-parser milestone validates source bytes only. |
| Strict-v0 locked-NAND source parser | One literal 228-state, 2,052-rule, nine-symbol finite machine with a compiled cubic bound | Accepts exactly valid source circuits, preserves valid bytes, rejects invalid bytes with empty output, cannot time out within the bound, and supplies polynomial-time witnesses plus leaf `RawRefinement`. Downstream milestones emit and expose the target reduction; this parser alone does not solve the target or prove P = NP. |
| Strict-v0 locked-NAND target emitter | One literal 1,387,921-rule, nine-symbol grammar-only controller with explicit all-input runtime and output-size bounds | Emits the exact direct target for grammar-decoded circuits, rejects malformed grammar with empty output, supplies compiled polynomial-time machine/function witnesses and exact leaf `RawRefinement`, and composes with the strict parser to compute `buildLockedNANDInstance`. The downstream reduction and M186 compatibility layer package and expose that composition; the emitter alone does not solve the target or prove P = NP. |
| Concrete strict-v0 locked-NAND polynomial reduction | A concrete polynomial many-one reduction from `EncodedNANDSAT` to `EncodedLockedNANDThreshold` built from the strict parser/emitter composition | Exact function identity, exact output, all-bitstring language equivalence, a `ReducesTo` witness, and recursive raw-machine refinement are formalized. Downstream milestones extend it from CNF-SAT and expose it report-facing; it does not establish NP-hardness, solve the target language, or prove P = NP. |
| General CNF-to-NAND semantic compiler | A total answer-independent transformation from strict canonical CNF formulas to intrinsically topological NAND circuits | Exact satisfiability preservation, edge semantics, gate count, quadratic serialized-output bound, malformed-input failure, and semantic composition with the locked-NAND builder are formalized. The following finite-machine milestone implements this function; the semantic theorem alone remains a pure semantic and size layer. |
| Fixed all-input CNF-to-NAND polynomial reduction | One fixed 135,070-rule three-node finite work graph implementing the semantic compiler on every bitstring | Exact output, non-timeout polynomial execution, `PolynomialTimeFunction`, literal `RawRefinement`, a direct `CNFSAT`-to-`EncodedNANDSAT` `PolynomialReduction`, and composition to `EncodedLockedNANDThreshold` are formalized. M186 exposes that exact route through the concrete report-facing names; it does not decide CNF-SAT, establish SAT NP-hardness or CNF-SAT NP-completeness, put CNF-SAT in P, or prove P = NP. |
| Report-facing concrete locked-NAND threshold reduction | `PNP.Main.locked_nand_threshold : ReducesTo CNFSAT EncodedLockedNANDThreshold`, witnessed by the composed finite parser, compiler, and emitter | Uniform all-bitstring correctness, polynomial runtime, output-size bounds, malformed-input behavior, and recursive raw refinement are linked to the report-facing theorem. Its closure uses only `Quot.sound` and `propext`, and M186 reuses it through the concrete compatibility layer. This is not a target decider, an NP-hardness or NP-completeness theorem, `ZeroSlack`, PCCMin, an eligible root theorem, or P = NP. |
| Universal verified residual-gain chain bound | A theorem for every finite proof-bearing chain and a Boolean verifier for disclosed executable chains | Semantics and the exhaustive reference minimum are preserved, endpoint slack plus length is bounded by starting slack, and the complete locked-NAND candidate admits at most four verified steps. This does not generate gains, prove completeness or stopping, construct `ZeroSlack`, compute a minimizer, prove polynomial runtime, discharge an assumption, or prove P = NP. |
| Global strict-gain stopping specification | A theorem over every finite direct-wire implementation, plus consequences for proof-bearing and executable verified-chain endpoints | Positive slack is equivalent to existence of a smaller equivalent implementation; zero slack and semantic minimality are equivalent to global absence. An endpoint is certified minimum only when separate global no-gain evidence is supplied. This is not an algorithm, finite-list completeness theorem, manuscript `ZeroSlack` certificate, polynomial runtime result, assumption discharge, or proof of P = NP. |
| Terminal full-carrier residual bridge | A direct-wire full-mode realization preserving complete multi-output semantics, with an independently stated terminal minimum | Terminalization preserves the exact implementation and gate count; the terminal minimum equals the exhaustive reference minimum; positive slack is equivalent to a cheaper whole-span full realization; and zero slack is equivalent to absence of one. The quotient carrier/firewall, proper and governed supports, saturation, `ZeroSlack`, PCCMin, and polynomial runtime remain absent. |
| Terminal quotient/full mode firewall | A computed ten-role profile, an explicit forgetful projection to selected coordinates, and a checked lift back to the complete profile | Projection preserves exact implementation, gate count, and complete semantics. A selected-coordinate comparison lifts exactly when every omitted coordinate agrees; retaining every coordinate lifts directly, and obligation discharge transports through a checked lift. This firewall alone does not compute a minimum or supply proper/governed supports, arbitrary manuscript quotient construction, saturation, `ZeroSlack`, PCCMin, or polynomial runtime. |
| Terminal full/quotient projection minima | Exhaustive finite scans for the smallest realization matching either the complete terminal profile or its selected quotient coordinates | Both minima are attained and universally minimal among matching realizations. Forgetting coordinates cannot increase the minimum; the full minimum is the quotient minimum plus an exact nonnegative defect, and that defect is zero exactly when an attained quotient minimum has a checked full lift. The scans range through the supplied implementation's gate count and are not a polynomial-time minimizer; proper/governed supports, saturation, `ZeroSlack`, PCCMin, and polynomial runtime remain absent. |
| Terminal projection transfer identity | Signed comparison of full and quotient minimum deltas across supplied meet, left, right, and join corners sharing one observer and projection | The exact Section 5.2 transfer identity holds. If the meet and both side defects are zero and the join defect is `D`, projection excess equals `D` and is positive when `D` is positive. This does not construct or certify a proper governed support square, prove saturation, complete `ZeroSlack` or PCCMin, or establish polynomial runtime. |
| Terminal saturation closure | Reflexive transitive closure of an explicitly supplied Boolean dependency relation over the finite universe of terminal primitive records | Generated saturation contains the seed, is dependency-closed and least among closed supersets, is monotone and idempotent, and fixes exactly the already closed supports. It does not derive the dependency relation from an arbitrary circuit, construct proper support, prove support completion or square legitimacy, instantiate a projection-compatible square, complete `ZeroSlack` or PCCMin, or establish polynomial runtime. |
| Terminal executable and physical support completion | Deterministic finite saturation work list plus canonical incoming-boundary and outgoing-interface wire scans for selected nodes in a finite direct-wire candidate | The work list computes exactly the inductive saturation; every crossing wire appears on the correct side, no unrelated wire appears, and the composed physical support is compatible. The dependency system is still explicit data; this is not proper positive support, manuscript-level support completion, square legitimacy, `ZeroSlack`, PCCMin, or a polynomial-runtime theorem. |
| Arbitrary terminal support extraction | Structural extraction of any supplied finite terminal record list, including a noncontiguous one, into an open direct-wire candidate over its canonical incoming boundary and ordered outgoing interface | The extracted candidate equals an independently defined open-support function for every boundary valuation and recovers the original selected interface values on whole-circuit-induced boundaries; extraction also composes with executable saturation. The record list and dependency system remain explicit inputs, so this is not derived proper positive support, governed support completion, square legitimacy, `ZeroSlack`, PCCMin, or a polynomial-runtime theorem. |
| Governed proper-positive terminal support search | Exhaustive finite search over every canonical primitive-record seed for a finite direct-wire candidate and explicit terminal dependency system | Each seed is saturated, physically completed, and extracted as an exact open support. Returned witnesses are nonempty, proper, and have exact positive local gain; a `none` result is equivalent to absence of such a seed in the canonical universe. The dependency system is explicit rather than circuit-derived, and the search is not a polynomial algorithm or a proof of global gain completeness, full manuscript support completion, square legitimacy, `ZeroSlack`, or PCCMin. |
| Saturated terminal support-square closure | Computed left, right, meet, and join corners from two finite seeds under an explicit terminal dependency system | The meet and join satisfy exact greatest-lower-bound and least-upper-bound laws, and every corner has exact extraction and whole-circuit recovery. This is finite closed-corner algebra, not the manuscript's projection-compatible square or square-legitimacy theorem. |
| Governed terminal support completion | Deterministic completion of any computed saturated support-square corner into its canonical physical boundary, ordered interface, and ten terminal profile-role lists | Exact membership, no duplicates, pairwise role disjointness, record coverage, dependency closure, physical compatibility, and exact corner retention are formalized. The dependency system remains supplied rather than circuit-derived; this is not obstruction routing, frontier pushout, the manuscript's projection-compatible square, square legitimacy, `ZeroSlack`, PCCMin, or a polynomial-runtime theorem. |
| Governed terminal frontier pushout | Canonical boundary, interface, and role-preserving profile gluing computed from the two governed side completions of a saturated support square | The independently completed join frontier equals the gluing, the meet profile is exactly the shared overlap, and every side physical coordinate remains external or is witnessed as internalized. The dependency system remains supplied rather than circuit-derived; this is not projection compatibility, side-tight minima, square legitimacy, complete obstruction routing, `ZeroSlack`, PCCMin, or a polynomial-runtime theorem. |
| Governed terminal projection square | Forgetful terminal projection of every governed corner and the side-only frontier pushout of a computed saturated support square | The exact physical frontier is retained, all ten role profiles are filtered exactly, and projection commutes with both the shared meet and the side-only pushout. The dependency system remains supplied rather than circuit-derived; this is not side-tight minima, square legitimacy, complete obstruction routing, `ZeroSlack`, PCCMin, or a polynomial-runtime theorem. |
| Side-tight four-corner minimum arithmetic | Componentwise comparison of meet, left, right, and join sizes against independently attained full or quotient minima | Exact minimum bounds and the signed four-slack identity are formalized, with a fail-closed Boolean and Option gate that returns the existing delta only when all four minima are attained. The canonical minima are independently attained; this is not one coherent four-corner basis, square legitimacy, complete obstruction routing, `ZeroSlack`, PCCMin, or a polynomial-runtime theorem. |
| Residual terminal four-corner carrier transport | One common ambient coordinate system for the governed and extracted meet, left, right, and join endpoints of every computed saturated terminal support square | Boundary, interface, and profile lists are duplicate-free; exact endpoints and meet/join profiles transport; present side physical coordinates are classified fail closed as retained or internalized. This does not transport four optimum realizers, construct a coherent optimum, prove square legitimacy, complete obstruction routing, `ZeroSlack`, PCCMin, or a polynomial-runtime theorem. |
| Residual terminal four-corner optimum carrier compatibility | One reversible ambient carrier for the independently attained meet, left, right, and join optimum realizations of every computed saturated terminal support square | Ambientization and localization preserve exact semantics and gate count, ambient and corner reference minima agree, and one shared observer and projection recover the canonical full and quotient optimum sizes. This does not prove coherent transport along the square legs, construct one coherent optimum, prove square legitimacy, complete obstruction routing, `ZeroSlack`, PCCMin, or a polynomial-runtime theorem. |
| Residual terminal four-corner optimum coherence dichotomy | Deterministic four-leg coherence check for any finite computed terminal support square, observer, projection, and full or quotient coherence mode | The classifier returns either one coherent canonical optimum tuple with exact transport facts or the exact first transport, semantic, profile, charge-profile, or mode mismatch. It does not prove every square coherent, construct the later no-outcome route, prove square legitimacy, complete obstruction routing, `ZeroSlack`, PCCMin, or a polynomial-runtime theorem. |
| Residual terminal four-corner tight-basis maximum | Complete finite Cartesian family of every exact profile-constrained minimum implementation at the four computed corners, filtered by the arbitrary-family coherence query | Under exact local route silence the family is nonempty, every retained signed incidence value equals the selected delta, and the full and quotient signed maxima equal that delta. This is a local all-finite maximum, not universal route silence, BN2 square legitimacy, complete obstruction routing, `ZeroSlack`, PCCMin, or a polynomial-runtime theorem. |
| Computed terminal BN2 square legitimacy | Exact governed frontier and forgetful-projection square computed from two finite seeds under one explicit terminal dependency system, direct-wire candidate, observer, and projection | The structural square is compatible, full and quotient minimum quantities share one carrier, and the final query returns either the complete local conclusion under exact route silence or the deterministic first proof-bearing route. The dependency system is not derived from an arbitrary circuit; universal route silence, the complete global no-outcome route, a BCEL anchor, unrestricted manuscript square legitimacy, `ZeroSlack`, PCCMin, and polynomial runtime remain absent. |
| Computed terminal BCEL anchor nucleus | Canonical minimum-cardinality positive anchor subfamily for an explicit finite terminal candidate, dependency system, governed proper-positive support, projection, observer, and positive whole-support projection defect | The classifier returns the exact first anchor-algebra, proper-cut defect, or local-route failure, or exact constant-cut and local BN2 conclusions for every proper cut. The dependency system and positive whole-support defect remain premises; manuscript activation and charge classes, the complete global route system, `SaturatePositive`, Package E, BCELReady, `ZeroSlack`, PCCMin, and polynomial runtime remain absent. |
| BN3 joint-realizability gap witness | A two-cut axiom-free logical model separating a realizing basis for each cut from one family that realizes both cuts stably | The witness still proves that arbitrary per-cut BN2 existence cannot imply a stable family. The successful computed BCEL nucleus now has a finite candidate-derived repair, but that repair is exhaustive and exponential and does not establish BN4-BN6, global `ZeroSlack`, or polynomial PCCMin. |
| Candidate-derived finite BN3 request envelope | Canonical request identities and a jointly selected full-or-quotient side-tight basis family over every proper cut of a successful computed finite BCEL anchor nucleus | Executable request membership is exact, monotone, and stable; minimal consumers are exact singletons; active incidence has no duplicates; and upstream failures remain proof-bearing. Proper cuts are generated through all subsets, so this is not a polynomial algorithm and does not supply BN4-BN6, complete global routing, selector/realizer completeness, `ZeroSlack`, or polynomial PCCMin. |
| Finite BN4 activation-exact cancellation kernel | Canonical singleton activation codes and exact same-key integer cancellation over a complete typed key | For an explicit caller-supplied cell ledger, Lean proves activation-code extensionality, exact positive/negative mass conservation, preserved key identity, positive residual mass, and no opposite-sign residual pair. The ledger, semantic signatures, and transport types are not derived from four-corner bases, so this is not the full historical BN4 theorem and does not supply PkgC, BN6, complete global routing, `ZeroSlack`, or polynomial PCCMin. |
| Finite BN5 full-shadow localization kernel | A total finite classifier for exact full-unit multiplicities against explicit quotient-shadow coordinates | Starting from an explicit negative BN4 cancellation result, payload list, cut, and shadow universe, Lean returns cut silence, complete multiplicity coverage, or a strict Hall deficit with exact coordinates and a local X1 route. It does not derive payloads or shadows from four-corner bases, connect complete matching back to a BN4 contradiction, prove the full historical BN5 diagnosis, or supply PkgC, BN6, complete global routing, polynomial runtime, `ZeroSlack`, or PCCMin. |
| Finite PkgC separating-consumer restoration dichotomy | A canonical arbitrary-finite classifier for explicit minimal-consumer antichains and exact-coordinate restorations | Lean returns exact V54 singletonization or the first disjoint nonsingleton pair, canonically indexes its atoms, and classifies an explicit restoration universe into complete multiplicity coverage or a strict Hall deficit with a local Q route. The antichain and restoration universe are not derived from terminal candidates, coverage is not connected to a BN4 or BN5 contradiction, the local route is not globally embedded, and full PkgC route silence, polynomial runtime, `ZeroSlack`, and PCCMin remain absent. |
| Finite PkgC typed restoration realization | A typed coordinate-preserving realization of every atom of the canonical first disjoint nonsingleton pair | Given an explicit minimal-consumer antichain and typed restoration operation, Lean materializes one candidate per atom, proves exact positional coordinates and full/shadow equality-fibre multiplicities, obtains complete coverage, excludes a strict Hall deficit, or proves V54 singletonization. The operation is not constructed from terminal candidates or proved fully semantically adequate, coverage is not connected to a BN4 or BN5 contradiction, local routes are not globally embedded, and full PkgC route silence, polynomial runtime, `ZeroSlack`, and PCCMin remain absent. |
| Finite PkgC typed-restoration same-key cancellation | A canonical opposite-sign unit-cell ledger for each quotient atom and its typed restored candidate | Given an explicit minimal-consumer antichain, typed exact-coordinate restorer, and coordinate maps, Lean proves each quotient/restored pair has the same nested BN4 key, balances every key, computes an empty residual and zero signed mass, or proves V54 singletonization. The generated cells are not yet identified with a terminal candidate's ambient BN4 ledger, and full PkgC route integration and silence, polynomial runtime, `ZeroSlack`, and PCCMin remain absent. |
| Finite PkgC ambient BN4 ledger embedding | A proof-bearing exact multiset decomposition of an explicit ambient BN4 ledger into the generated balanced PkgC cancellation subledger plus an explicit remainder | Lean preserves duplicates, decomposes positive and negative mass at every key, equates ambient signed mass and executable residual contribution with the remainder, retains candidate-derived canonical atoms, and derives V54 singletonization when every binding exists and no computed bridge exists. The ambient ledger, restorer, certificate or serialization, and successful kernel remain explicit inputs; global route integration and silence, polynomial runtime, `ZeroSlack`, and PCCMin remain absent. |
| Finite PkgC ambient BN4 residual reduction | A per-key and complete-ledger reduction of an explicit ambient BN4 ledger after exact removal of the generated balanced PkgC subledger | Lean proves the executable residual cell at every complete key and the complete canonical executable residual ledger equal the explicit remainder, covers every remainder key, constructs the exact reduction with a fail-closed classifier, and derives an empty residual ledger from an empty remainder. The ambient ledger, restorer, exact embedding, and explicit remainder remain proof-bearing; the remainder is not proved empty or route-producing, and global route integration and silence, polynomial runtime, `ZeroSlack`, and PCCMin remain absent. |
| Finite V54 consumer-antichain normal form | An arbitrary-finite normal form for two-sided cut activation over an explicit antichain of minimal consumers | Lean proves nonzero activation exactly equivalent to a disjoint consumer pair and, under an explicit singletonization premise, literal equality with the singleton-footprint cut indicator. The finite BN6 bridge transports explicitly grouped instances into V53, and the PkgC classifier proves singletonization when no separating pair exists, but full PkgC construction and route silence, terminal-candidate derivation and grouping, complete global routing, polynomial runtime, `ZeroSlack`, and PCCMin remain absent. |
| Finite V53 constant-cut hypergraph rigidity | The complete classification of a finite sparse positive weighted hypergraph whose nonempty proper cuts all have one common value | Lean proves the two-anchor full-span equation, the three-anchor common-pair equation, and zero proper-footprint weight for four or more anchors. The finite BN6 bridge constructs this hypergraph and cut equation from explicit grouped V54 cells, but full PkgC construction and route silence, terminal-candidate derivation and grouping, the full historical BN6 and Packet result, complete global routing, polynomial runtime, `ZeroSlack`, and PCCMin remain absent. |
| Finite BN6 grouped hypergraph-packet bridge | An arbitrary-finite transport from explicit grouped positive payload-bearing V54 survivors into the exact V53 cut sum and packet classification | Lean returns the pair, mixed three-anchor, or full-span classification with source payload witnesses. The survivor family, grouping, PkgC singletonization proofs, payloads, and constant-cut equation are explicit; full PkgC completion, their derivation from terminal candidates, the full historical BN6 and Packet selector and realizer results, complete global routing, polynomial runtime, `ZeroSlack`, and PCCMin remain absent. |
| Finite Packet selector-seed extraction | An arbitrary-finite extraction of raw selector inputs from an exact BN6 packet conclusion | Lean returns a carrier-contained, payload-backed raw seed at each selector-relevant positive footprint, including the mixed three-anchor alternative without assuming both masses are positive. The packet conclusion is explicit; selector-universe membership, faithfulness, compatibility, enumeration, a realizer, a route, polynomial generation and runtime, complete PkgC, `ZeroSlack`, and PCCMin remain absent. |
| Finite Packet payload-selector universe membership | The exact duplicate-free footprint list already present in an arbitrary finite explicit grouped BN6 family | Lean proves membership equivalent to an original grouped cell at the same footprint and upgrades every payload-backed pair, balanced-triple pair, or full-span seed into that universe. The grouped family is explicit. This is not the manuscript's encoded or polynomial selector universe; selector faithfulness, compatibility, a realizer, a route, derivation from terminal data, polynomial enumeration and size bounds, complete PkgC, `ZeroSlack`, and PCCMin remain absent. |
| Canonical finite Packet selector handles | Input-relative positions in the exact duplicate-free grouped-footprint universe of an arbitrary finite explicit grouped BN6 family | Lean proves exact decoding injective, every payload selector has a unique handle, and each decoded footprint retains payload evidence, stays in the carrier, and has length at least two. These list positions are not the manuscript's bit encoding or a polynomially enumerable selector universe; manuscript-level selector faithfulness and compatibility, a realizer or route, terminal-data derivation, polynomial encoding bounds and runtime, complete PkgC, `ZeroSlack`, and PCCMin remain absent. |
| Canonical fail-closed Packet selector-handle codec | Unary bitstrings for canonical input-relative selector handles, with a total decoder | Lean rejects missing delimiters, trailing data, and out-of-range indices and proves round trip, injectivity, canonical decoding, exact and explicit-list-bounded length, unique accepted codes, and retained payload, carrier, size, cell, and atom evidence. The supplied grouped-family list is not bounded by encoded circuit size; polynomial enumeration and runtime, atom or payload encoding, selector faithfulness and compatibility, a realizer or route, terminal-data derivation, complete PkgC, `ZeroSlack`, and PCCMin remain absent. |
| Total fail-closed Packet source-payload realization | A total function from accepted canonical selector codes to the exact decoded handle, original cell, footprint, and a canonical original positive payload atom | Lean proves exact re-encoding, supplied-family membership, strict atom positivity, equivalence with the finite payload-selector predicate, and preservation of all Packet branches. This is source-payload materialization relative to an explicit grouped family, not a manuscript gain-or-blocker selector realizer. The code still denotes only a list position; payload serialization, a replacement circuit, selector faithfulness and compatibility, a gain or typed blocker route, terminal-data derivation, circuit-size bounds, polynomial generation and runtime, complete PkgC, `ZeroSlack`, and PCCMin remain absent. |
| Conditional HB selector-silence rank closure | Strong finite-rank induction over an accepted supplied Packet typed-realizer table after checked HN/BUD inactivity and explicit exclusion of every strict equivalent gain | Lean proves every canonical selector in that accepted table nonfaithful. A specialization obtains the gain-exclusion premise from a supplied global gain-coverage certificate plus exact source-cell no-gain. The family, selector table, ranks, faithfulness predicate, realizer claims, blocker data, dependencies, certificate, and semantic premise remain explicit inputs. This is not selector faithfulness or compatibility, unconditional HB negative closure, unconditional global silence, unconditional `ZeroSlack`, PCCMin, or a polynomial-runtime result. |
| Executable HB selector-silence induction | An exhaustive all-row check over an accepted supplied Packet typed-realizer table, followed by checked HN/BUD inactivity and strong finite-rank induction | Lean proves every canonical realizer claim is a typed bottom, retains faithful-row validity, removes HN/BUD bottoms, and eliminates faithful strictly lower-rank seeds, so every canonical selector is nonfaithful without global semantic no-gain as a theorem premise. The family, selector table, ranks, faithfulness predicate, exhaustive claims, blocker data, dependencies, and rank map remain explicit inputs. This is not selector faithfulness or compatibility, an independently constructed realizer, unconditional HB negative closure, unconditional `ZeroSlack`, PCCMin, or a polynomial-runtime result. |
| Packet selector-faithfulness routing | A ten-field executable route predicate on each canonical positive source payload, exhaustively checked across a supplied grouped BN6 family and bound exactly to a supplied HB faithfulness table | Lean exposes the first failed route, turns every positive Packet conclusion into a faithful canonical handle when all route checks and the exact table binding pass, and contradicts accepted executable selector silence for that same handle. The family, payload fields, rank tags, HB table, claims, blocker data, dependencies, and rank map remain explicit inputs. This is not terminal construction of those inputs, positive slack, selector compatibility, unconditional HB negative closure, unconditional `ZeroSlack`, PCCMin, or a polynomial-runtime result. |
| Canonical Packet faithfulness-table construction | A constructor over an arbitrary finite explicit grouped BN6 family and finite rank carrier that replaces the free HB faithfulness function with the canonical positive source-payload computation | Lean proves that the constructed table preserves the supplied rank map, HN/BUD activity, and every realizer claim exactly, makes exhaustive faithfulness binding true by construction, and yields the positive-Packet versus executable-silence contradiction without a separate binding premise. The grouped family, ten payload fields, finite rank tags and assignment, route-clear evidence, claims, activity, dependency rows, and rank map remain explicit inputs. This is not terminal derivation of those inputs, full external selector compatibility, complete route silence, unconditional HB negative closure, positive slack, `SaturatePositive`, BCELReady, unconditional `ZeroSlack`, PCCMin, encoded-size or polynomial-runtime bounds, SAT in P, removal of a project assumption, or P = NP. |
| Rank-reflected Packet descent route | A constructor that replaces the final caller-supplied payload Boolean with the exact ten-coordinate RankWF comparison while preserving the first nine fields | Lean proves acceptance carries actual rank descent and a forced final descent route carries actual nondecrease; the positive Packet/HB endpoint returns that proof or an earlier exact field route without route-clear or descent-binding premises. The first nine fields, before/after ranks and handle assignment, grouped family, selector-rank map, claims, activity, dependencies, and rank map remain explicit inputs. This does not prove external semantics for the other nine routes, map them into a complete global outcome system, guarantee a decreasing transition, construct the no-lower ledger, establish full external selector compatibility or complete route silence, prove unconditional HB negative closure or `ZeroSlack`, establish PCCMin or polynomial runtime, remove a project assumption, or prove P = NP. |
| Checked Packet budget/HB activity binding | An executable all-handle check mapping every typed Packet budget mismatch to HB budget-node activity at the table-owned handle rank | With the supplied checked well-founded HB no-outcome closure, Lean proves typed budget equality for every canonical handle and excludes the Packet budget route. The grouped family, binding, typed budget values, rank map, activity environment, dependency table, and closure premise remain explicit. This is not a terminal-data construction of Bud(u), BudgetResolve, blocker semantic completeness, full Packet adequacy, unconditional HB negative closure, complete route exclusion, `ZeroSlack`, PCCMin, or a polynomial-runtime result. |
| Checked Packet semantic/HN activity binding | An executable all-handle check mapping any failure of simultaneous frontier, obligation, activation, and direction agreement to HN-node activity at the table-owned handle rank | With the supplied checked well-founded HB no-outcome closure, Lean proves all four typed semantic fields agree. The separate budget/HB binding and executable selector silence then exclude every semantic first route and leave exact residual nondecrease. The grouped family, typed coordinates, activation atoms, direction and budget values, bindings, rank map, activity environment, dependency table, and closure premise remain explicit. This is not terminal-data construction, HN blocker semantics, semantic dependency completeness, complete Packet adequacy, a decreasing transition, a no-lower contradiction, unconditional HB negative closure, `ZeroSlack`, PCCMin, or a polynomial-runtime result. |
| Checked Packet descent no-lower binding | An executable all-handle check accepting exactly when no fully computed first route is residual nondecrease | A supplied positive Packet conclusion, checked semantic/HN and budget/HB bindings, selector silence, and well-founded HB no-outcome closure force the local checker to reject; accepting the same row is contradictory. The family, rank map, residual ranks, typed fields, realizer and dependency tables, bindings, silence, and closure checks remain explicit. This is one local row, not terminal-data construction, the complete no-lower ledger, HResolve, BudgetResolve, normalization, named descent routing, saturation, replay, unconditional HB closure, `ZeroSlack`, PCCMin, or a polynomial-runtime result. |
| Checked Packet no-lower ledger | One executable Boolean composing the exact semantic/HN binding, budget/HB binding, selector-silence, HB no-outcome closure, and descent/no-lower checks | A positive Packet forces rejection, while accepted ledger data excludes a positive Packet conclusion for the same supplied inputs. This closes the Packet branch only. The family, coordinates, values, ranks, realizer claims, activity environment, dependency rows, and terminal data remain supplied; the complete no-lower ledger, HResolve, BudgetResolve, normalization, other named obstruction routing, saturation, replay, unconditional HB closure, `ZeroSlack`, PCCMin, and polynomial runtime remain open. |
| Checked finite HResolve coverage ledger | A fixed-priority exact/gain/blocked/unresolved classifier over an arbitrary supplied finite candidate family, with a generated route ledger and independently checked duplicate-free `NoHereditary` sidecar | Lean proves the ledger sound and complete for that enumeration and proves sidecar acceptance excludes exact and gain for every listed candidate. The candidate universe and decidable exact, gain, and blocker predicates remain supplied; HN grammar, BWL exactness, H-disjointness, exact-minimum, strict-gain, and blocker dependency semantics are not established. This is not full HResolve, the complete no-lower ledger, BudgetResolve, unconditional HB or `ZeroSlack`, PCCMin, or a polynomial-runtime result. |
| Terminal-derived HResolve support resolver | The complete duplicate-free family of canonical terminal support seeds for every finite direct-wire candidate and profile-width model, with candidate-derived saturation and an exact/gain classifier | Lean computes either an exact route carrying semantic-minimum evidence or a gain route carrying a strict-equivalent-gain witness for every canonical support. The complete finite reference search may be exponential. It does not implement manuscript HN grammar, BWL exactness, H-disjointness, blocker or `NoHereditary` semantics, polynomial HResolve, BudgetResolve, the complete no-lower ledger, unconditional `ZeroSlack`, PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| Finite terminal budget-envelope resolver | A complete scan of canonical terminal support seeds under candidate-derived saturation and supplied natural gate and saturated-record caps | Lean recomputes nonempty gate and interface feasibility plus both caps and returns a feasible semantic minimum, feasible strict equivalent gain, or complete `NoBudget` exclusion for every seed. The caps remain supplied and the exhaustive search may be exponential. This is not manuscript HN/BUD grammar, BWL or its budget-envelope dynamic program, blocker semantics, polynomial BudgetResolve, the complete no-lower ledger, unconditional `ZeroSlack`, PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| Finite terminal budget no-lower ledger | The complete exact/gain/`NoBudget` route ledger over all canonical terminal supports for a finite direct-wire candidate under candidate-derived saturation and supplied natural resource caps | Lean proves ledger soundness and completeness and proves that an accepted ledger makes every budget-feasible governed support a semantic minimum while excluding every feasible strict-equivalent gain. The caps remain supplied and the exhaustive search may be exponential. This closes only the finite terminal-derived budget branch; it is not manuscript HN/BUD grammar, BWL or its budget-envelope dynamic program, blocker semantics, polynomial BudgetResolve, Packet or complete no-lower composition, unconditional `ZeroSlack`, PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| Finite terminal Packet-budget no-lower composition | One Boolean recomputing the complete canonical budget-support no-lower ledger and the checked five-row Packet no-lower ledger over the same direct-wire candidate | Acceptance makes every governed budget-feasible canonical support a semantic minimum, excludes every feasible strict-equivalent gain, and excludes a positive Packet conclusion. The caps, Packet family, payloads, ranks, claims, activity environment, and dependency tables remain supplied. This is a finite two-branch composition, not the complete no-lower ledger, manuscript HN/BUD grammar, polynomial HResolve or BudgetResolve, unconditional `ZeroSlack`, PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| Certified-path BWL minimum | The exact lexicographic minimum by realized cost, residual rank, frontier deviation, and direct-wire code over one nonempty finite supplied family of certified hereditary paths | Lean preserves semantic and frontier fidelity, nonempty block coverage, and one of four HN shapes, and an explicit completeness premise extends the lower bound to every path in the supplied governed predicate. The family, predicate, and premise remain supplied. This is not terminal path derivation, grammar soundness or completeness, LN confluence, ParseOrExit, independent leaf tightness, the full BWL theorem, full HResolve, `ZeroSlack`, PCCMin, or a polynomial-generation result. |
| HResolve certified-path family | A duplicate-free maximal pairwise H-disjoint selection over a finite supplied family of proof-bearing hereditary candidates | Every selected candidate preserves an exact certified-path minimum and supplied footprint-coherence evidence; every rejection carries a selected first interference route. The candidates, paths, footprints, governed predicates, completeness proofs, and coherence proofs remain supplied. This is not terminal derivation, HN grammar completeness, the full NoHereditary sidecar, blocker-to-HB-rank semantics, full or polynomial HResolve, `ZeroSlack`, or PCCMin. |
| HResolve ZeroSlack sidecar | Checked report-facing NoHereditary evidence over an arbitrary finite supplied governed HResolve family | Lean recomputes duplicate-free total blocked coverage, excludes exact and gain routes for every listed candidate, and binds those routes to semantic minimum and strict equivalent gain propositions. The family, implementation map, predicates, decidability, and blocker semantics remain supplied. This is not hereditary-candidate derivation, H0-H4 blocker semantics, full or polynomial HResolve, unconditional `ZeroSlack`, or PCCMin. |
| Budget ZeroSlack sidecar | Checked report-facing NoBudget evidence over arbitrary finite direct-wire candidates and supplied natural caps | Lean records the failed exhaustive terminal-envelope search, excludes every budget-feasible canonical support, and separately binds exact and gain routes to semantic minimum and strict equivalent gain propositions. The caps remain supplied and the search may be exponential. This is not B0-B4 Budget grammar or blocker semantics, full or polynomial BudgetResolve, unconditional `ZeroSlack`, or PCCMin. |
| Selector/HB ZeroSlack sidecar | One checked report-facing certificate over supplied grouped BN6, typed-realizer, and exact-rank HB dependency tables | Exact executable checks yield all-canonical-selector nonfaithfulness, exact typed-bottom rows, complete no-outcome closure, all-node HN/BUD inactivity, and well-founded dependencies. The grouped family, tables, environment, claims, activity bits, dependency rows, and rank map remain supplied. This is not selector compatibility, blocker semantics, semantic dependency completeness, the BCEL contradiction, unconditional `ZeroSlack`, or PCCMin. |
| Packet/budget no-lower ZeroSlack sidecar | One checked report-facing certificate over supplied same-candidate Packet and terminal-budget data | The exact executable composition proves every governed budget-feasible support semantically minimum, excludes every such strict equivalent gain, and excludes a positive Packet conclusion for the supplied family. The caps, model, family, payloads, ranks, claims, environment, dependency rows, and rank maps remain supplied. This finite two-branch result is not the complete manuscript no-lower ledger, unconditional `ZeroSlack`, PCCMin, or polynomial runtime. |
| BCEL/Packet no-lower ZeroSlack sidecar | One checked dependent certificate over a supplied M180 no-lower certificate and an exact carrier-at-least-two decision | BCEL constant activation would construct the positive BN6 Packet excluded by the accepted same-family certificate, so constant activation is impossible under those supplied inputs. It does not derive constant activation from positive residual slack, construct BCELReady, complete the manuscript no-lower ledger, establish unconditional `ZeroSlack`, prove PCCMin or polynomial runtime, or prove `P = NP`. |
| Same-family Selector/HB, Packet, and BCEL ZeroSlack coherence | Seven reviewed theorem pins derive Selector/HB acceptance and exact family, realizer-table, and dependency-table identities from one supplied accepted M180 certificate and carry them through its dependent M181 BCEL boundary | The report-facing Selector/HB, Packet, and BCEL evidence cannot be assembled from detached certificate families. The grouped family and all terminal, budget, Packet, realizer, dependency, rank, and BCEL data remain supplied; this is not unconditional `ZeroSlack`, PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| Same-candidate finite BCEL-ready and Packet carrier coherence | Five reviewed theorem pins use a checked bijective anchor map and exact list equality to identify the computed finite BCEL-ready nucleus with the exact grouped Packet carrier for the same candidate and model | The inherited carrier-size bound and existing positive-Packet and constant-activation exclusions apply to one coherent family. The terminal problem, positive full-slack premise, Packet family, map, and downstream certificate data remain supplied. This does not identify activation weights with projection excess, derive constant activation from positive residual slack, complete the no-lower ledger, establish unconditional `ZeroSlack`, prove PCCMin or polynomial runtime, or prove `P = NP`. |
| Finite BCEL and Packet activation-coherence obstruction | Six reviewed theorem pins recompute the selected nucleus defect, Packet cut value, and all canonical nonempty proper-cut activations, then prove a deterministic classifier returns a proof-bearing mismatch | Full activation coherence would identify mapped activation with projection excess and contradict the existing same-family Packet exclusion. This diagnoses that the supplied finite data are incoherent; it does not construct or repair activation coherence. The family, map, activation data, and downstream certificates remain supplied, and exhaustive cut enumeration may be exponential. This is not unconditional `ZeroSlack`, PCCMin, polynomial runtime, SAT in P, or `P = NP`. |
| Concrete report-facing locked-NAND compatibility | Six reviewed theorem pins identify the public SAT/locked-NAND languages and complexity interfaces with the concrete finite-pipeline definitions and directly reuse the checked all-bitstring polynomial reduction | Removes the duplicate locked-NAND project axiom and caller-supplied reduction edge. It is not a target decider, SAT NP-hardness transport, unconditional residual minimisation, ZeroSlack, exact polynomial PCCMin, the eligible root theorem, or `P = NP`. |
| Concrete residual-band compatibility | Seven reviewed theorem pins identify the public residual-band endpoint with the concrete fail-closed encoded exact-minimum threshold language, prove arbitrary typed-candidate reference-minimum semantics, and replace the supplied locked-to-residual edge with a compiled identity reduction | Removes `PNP.ResidualBandExactMinimization` from project-specific proof authority. Exhaustive reference minimisation is not a polynomial-time PCCMin algorithm. This is not residual-band promise closure, unconditional `ZeroSlack`, deterministic CNFSAT in P, SAT hardness transport, the eligible root theorem, or `P = NP`. |
| Typed PCCPack reflection | One reviewed completion-theorem pin verifies typed transparent generation, structural identifier acceptance, mismatch rejection, and exact projection of every explicitly supplied proof-bearing `PCCMinLoopCertificate`; 15 declarations are audited | Removes `PNP.GeneratePCCPack`, `PNP.CheckPCCPackexp`, and the caller-supplied reflection field from active proof authority. It does not construct the certificate, verify historical JavaScript package bytes or semantic string fields, implement PCCMin, prove unconditional `ZeroSlack` or polynomial runtime, put CNFSAT in P, establish SAT hardness transport, create the eligible root theorem, or prove `P = NP`. |
| Proof-bearing PCCMin total-oracle loop | One axiom-free reviewed completion-theorem pin proves total well-founded strict-gain recursion for every finite direct-wire implementation under an explicit proof-bearing total oracle, preserving semantics and returning exact-minimum and zero-residual-slack evidence with a starting-slack iteration bound; eight declarations are audited | The oracle remains supplied, and the regression fixture obtains it by exhaustive reference minimisation. This is not an executable or encoded-size polynomial PCCMin construction, terminal-route derivation, unconditional `ZeroSlack`, deterministic CNFSAT in P, SAT hardness transport, a global-gate closure, the eligible root theorem, or `P = NP`. |
| Proof-bearing PCCMin normalization and oracle composition | One axiom-free reviewed completion-theorem pin composes an explicit proof-bearing total normalizer with the explicit proof-bearing total oracle for every finite direct-wire implementation; ten declarations are audited | Non-increasing semantic normalization lifts later gains to strict gains from the original implementation and transports exact-minimum and ZeroSlack endpoints into the checked terminating loop. Both stages remain supplied, and the regression fixtures use exhaustive reference minimisation. This is not an executable or encoded-size polynomial PCCMin construction, terminal-route derivation, unconditional `ZeroSlack`, deterministic CNFSAT in P, SAT hardness transport, a global-gate closure, the eligible root theorem, or `P = NP`. |
| Rank-ordered proof-bearing PCCOracle orchestration | One reviewed completion-theorem pin enforces HResolve, BudgetResolve, then every selector in every canonical finite rank row, with immediate gain return and complete typed silence before ZeroSlack; its closure uses only Lean-standard `Quot.sound` and `propext`, and 15 declarations are audited | The normalizer, resolvers, rank rows, selector universe, realizer, typed blocker semantics, and ZeroSlack closure remain supplied, and the exhaustive reference fixture is not polynomial. This is not an executable or encoded-size polynomial PCCMin construction, terminal-data derivation, unconditional `ZeroSlack`, deterministic CNFSAT in P, SAT hardness transport, a global-gate closure, the eligible root theorem, or `P = NP`. |
| Checked Packet-backed PCCMin rank-selector construction | One reviewed completion-theorem pin checks a data-only unit-charge gain or typed HN, budget, or lower-seed blocker at every canonical handle, derives exact-rank rows from the supplied table-owned rank map, and reuses the M191 oracle and loop; its closure uses only Lean-standard `Quot.sound` and `propext`, and 15 declarations are audited | The grouped terminal family, rank assignment, claim table, resolvers, normalizer, blocker semantics, and ZeroSlack closure remain supplied, and the exhaustive reference fixture is not polynomial. This is not terminal-object or claim derivation, an executable or encoded-size polynomial PCCMin construction, unconditional `ZeroSlack`, deterministic CNFSAT in P, SAT hardness transport, a global-gate closure, the eligible root theorem, or `P = NP`. |
| Checked Packet/HB conditional ZeroSlack bridge | One reviewed completion-theorem pin derives complete selector silence from checked Packet claims and exact rank rows, applies checked HB no-outcome closure to eliminate every canonical faithful handle, and reaches conditional ZeroSlack through one explicit positive-residual-slack-to-faithful-selector premise before reusing the M192 selector construction and loop; its closure uses only Lean-standard `Quot.sound` and `propext`, and 15 declarations are audited | The positive-slack bridge, grouped terminal family, rank assignment, claim table, resolvers, normalizer, blocker semantics, encoded-size bounds, and exhaustive reference fixture remain supplied. This is not terminal-object or bridge derivation, an executable or encoded-size polynomial PCCMin construction, unconditional `ZeroSlack`, deterministic CNFSAT in P, SAT hardness transport, a global-gate closure, the eligible root theorem, or `P = NP`. |
| BN6 computed-faithfulness checked Packet/HB bridge | One reviewed completion-theorem pin carries positive residual slack into the general BN6 Packet theorem under an explicit constant-activation premise, computes a faithful selector from checked route-clear payload data, and uses the M193 checked HB contradiction to derive conditional ZeroSlack before reusing the checked loop; its closure uses only Lean-standard `Quot.sound` and `propext`, and 12 declarations are audited | Constant activation, the grouped terminal family, carrier lower bound, payload and rank construction, route-clear proof, claim and HB tables, resolvers, normalizer, blocker semantics, encoded-size bounds, and the exhaustive reference fixture remain supplied. This is not manuscript-wide SaturatePositive or BCELReady, terminal-object or activation derivation, an executable or encoded-size polynomial PCCMin construction, unconditional `ZeroSlack`, deterministic CNFSAT in P, SAT hardness transport, a global-gate closure, the eligible root theorem, or `P = NP`. |
| Same-candidate BCEL activation-route classifier and conditional ZeroSlack bridge | One reviewed completion-theorem pin returns a proof-bearing carrier, cut-value, or proper-cut activation mismatch, or derives coherent constant activation, identifies it with the BCEL projection excess, and reuses M194 for conditional ZeroSlack; its closure uses only Lean-standard `Quot.sound` and `propext`, and 15 declarations are audited | The terminal problem, positive premise, checked finite BCEL-ready certificate, grouped family, payloads, tables, ranks, route-clear proof, resolvers, normalizer, blocker semantics, encoded-size bounds, and exhaustive powerset classifier remain supplied. Mismatch diagnostics are not constructed gains or globally decreasing transitions. This is not manuscript-wide SaturatePositive or BCELReady, terminal-object derivation, an executable or encoded-size polynomial PCCMin construction, unconditional `ZeroSlack`, deterministic CNFSAT in P, SAT hardness transport, a global-gate closure, the eligible root theorem, or `P = NP`. |
| Sparse V53 constant-cut basis and checked conditional ZeroSlack bridge | One reviewed completion-theorem pin proves a shape-specific basis equivalent to all nonempty proper-cut equations, checks it without enumerating carrier subsets, and derives constant activation plus conditional ZeroSlack on acceptance; its closure uses only Lean-standard `Quot.sound` and `propext`, and 22 declarations are audited | Terminal data, raw cells and payloads, tables, ranks, route-clear proof, resolvers, normalizer, blocker semantics, upstream construction, and complete encoded-size bounds remain supplied. Basis rejection is not a constructed gain. This is not manuscript-wide SaturatePositive or BCELReady, terminal-object derivation, executable polynomial PCCMin, unconditional `ZeroSlack`, deterministic CNFSAT in P, SAT hardness transport, a global-gate closure, the eligible root theorem, or `P = NP`. |
| Sparse V53 proper-cut activation route and checked conditional ZeroSlack bridge | One reviewed completion-theorem pin proves that the duplicate-free singleton and order-preserving pair proper cuts form a quadratic-size family equivalent to all nonempty proper-cut equations, returns the first exact small-cut mismatch or derives the equation, and reflects a mismatch through the direct raw ledger or reaches conditional ZeroSlack; its closure uses only Lean-standard `Quot.sound` and `propext`, and 30 declarations are audited | Terminal data, raw cells and payloads, tables, ranks, route-clear proof, resolvers, normalizer, blocker semantics, upstream construction, and complete encoded-input bounds remain supplied. The local quadratic list bound is not a complete polynomial runtime theorem, and the mismatch is not a constructed gain or globally decreasing transition. This is not manuscript-wide SaturatePositive or BCELReady, terminal-object derivation, executable polynomial PCCMin, unconditional `ZeroSlack`, deterministic CNFSAT in P, SAT hardness transport, a global-gate closure, the eligible root theorem, or `P = NP`. |
| Fixed residual terminal RankWF | Exactly ten natural-number coordinates in the manuscript priority order, with an executable comparison equivalent to nested lexicographic order | Lean proves every priority witness, proof-bearing descent, accessibility, induction, and well-foundedness. It does not map current routes into the complete global outcome system, prove that a current route decreases the rank, establish route completeness or Package E, or prove `ZeroSlack`, PCCMin, or polynomial runtime. |
| Concrete publication gate | Strict conjunction requiring an eligible concrete standard complexity target, exact compatibility-root theorem, reviewed non-null fingerprints, source closure, and fixed Lean-standard axiom closure | False; the eligible charged-pipeline target definition is present but inactive, while the root and activation fingerprints are absent |
| `PNP.PEqualsNP` compatibility alias | Exact alias of the concrete finite charged-pipeline mutual-inclusion proposition | A proposition definition, not a proof; it cannot substitute for the absent eligible root theorem `PNP.Main.p_eq_np` |
| `unresolved` | Executable explicit-list scan found no strict equivalent gain in the supplied finite list | Does not exclude unlisted gains and does not imply global minimality or `ZeroSlack` |
| Current canonical report | 142-page status report generated from the inventory and publication map | Non-claiming; current aliases point here |
| Historical manuscript | Former 57-page direct-claim report at the pinned 7072f8d source tag | Audit material only; not current authority or a canonical download |

## Core Claim Terms

| Term | Canonical source in this checkout | Definition for review | Standard concept | Minimal example | Enforcing code path or audit target | Failure mode to test | Uncertainty status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Historical checker claim | [README.md](../README.md), report "Executive status and claim boundary" | The report claimed `P = NP` through the conditional assertion `CheckPCCPackexp(GeneratePCCPack())=accept => P = NP` and recorded antecedent acceptance in a sealed artefact stack. This is superseded assertion-checker evidence, not current proof status. | Historical claimed theorem with a machine-checking antecedent. | None locally beyond reading the historical fields. | Not executable from this checkout. `sourceRef`: `pcc-check-pcc-pack-exp0.mjs`, `pcc-runall0.mjs`, `pcc-final-proof-report0.mjs`. `docsRef`: `REPRODUCE.md`, `REVIEWER_MAP.md`. `artifactRef`: generated proof-report bundle. | The recorded field, antecedent, or replay linkage differs, or historical acceptance is presented as a formal proof. | Generated artefact required; independent source audit required; independent mathematical review required. Current authority remains the formal-reconstruction status. |
| Verification status | [README.md](../README.md), [docs/trust_model.md](trust_model.md), [docs/reproducibility.md](reproducibility.md) | This checkout can verify public file identity, fixture pass/fail behavior, audit-coordinate consistency, and documentation links. It cannot verify theorem correctness or checker soundness. | Verification boundary or assurance case. | `npm test` runs local smoke checks; `npm run test:audit-targets` validates cross-repo coordinates when a sibling checkout is available. | Local: `npm run verify:seal`, `npm run examples:minimal`, `npm run test:negative`, `npm run test:docs`, `npm run test:audit-targets`. | A public doc implies these checks prove the theorem. | Confirmed in public checkout. |
| Source/checker revision | [downloads/source-checker-release.json](../downloads/source-checker-release.json), [docs/source_checker_map.md](source_checker_map.md) | The source/checker revision named for full audit is `sourceRef`, tag `final-pnp-proof-report-hardened-7072f8d` at commit `7072f8d0bda6d44d240f9bb3fad624fd357e1278`. Release docs and generated artefacts are separate refs. | Provenance reference. | Not executable in this checkout. | `publicCheckout`: `downloads/source-checker-release.json`. `docsRef`: `REPRODUCE.md`, `REVIEWER_MAP.md`. No `CURRENT_RELEASE.md` target is listed because it is absent at the pinned refs inspected here. | The referenced repo is unavailable, the tag resolves differently, release docs contain stale identifiers, or generated artefacts are assigned to the wrong ref. | Confirmed in public checkout for manifest values; independent source/docs/artefact coordinate validation required. |
| PCCPack | Report "Executive status and claim boundary", "CheckPack", "Generated package contract" | A finite proof-carrying package emitted by `GeneratePCCPack()` and accepted by `CheckPCCPackexp0` according to the report. | Proof-carrying package or certificate bundle. | [pass-basic-all-invariants.json](../examples/minimal/pass-basic-all-invariants.json) has a toy `pccpack` object with version, rows, and ledgers. | Local fixture: `checkPccpack` checks only schema/version, rows, acyclic import flag, and duplicate-row flag. `sourceRef`: `pcc-pack-concrete-materialized0.mjs`, `pcc-check-pcc-pack-exp0.mjs`. | Unknown schema, missing rows, import cycle, duplicate row key, invalid generated package acceptance. | Defined in pinned source; independent source audit required. Generated artefact required. Local fixtures are not proof evidence. |
| Generated package | Report "Final acceptance run and deterministic generator" | The package generated by `GeneratePCCPack()`; the report states that the generator is untrusted and the checker validates the generated bytes/records. | Generated proof artefact; untrusted build output. | Toy `pccpack` fixture. | Not executable from this checkout. `sourceRef`: `pcc-check-pcc-pack-exp0.mjs`. `docsRef`: `REPRODUCE.md`. `artifactRef`: generated proof-report bundle. | Generator output is trusted without canonical-byte checking, or replay accepts stale/generated-different bytes. | Generated artefact required; defined in pinned source; independent source audit required. |
| Accepted package boundary | Report "Claim boundary", "Final theorem extraction" | The claimed implication boundary is accepted generated-package checking plus replay/certificate/release-gate linkage. | Trust boundary around a machine-checked artefact. | No local theorem-level example. | Not executable from this checkout. `sourceRef`: `pcc-check-pcc-pack-exp0.mjs`, `pcc-runall0.mjs`, `pcc-final-proof-report0.mjs`. `artifactRef`: final proof-report bundle. | A conclusion is emitted without accepted package, replay, certificate, and release gate. | Generated artefact required; independent source audit required; independent mathematical review required. |

## Mathematical And Construction Terms

| Term | Canonical source in this checkout | Definition for review | Standard concept | Minimal example | Enforcing code path or audit target | Failure mode to test | Uncertainty status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Direct-wire word | Report "Base direct-wire framework" | A NAND circuit representation `W=(g_1,...,g_m,o)` where each gate is NAND of boundary coordinates, carrier constants, or earlier gate outputs, and `o` is an ordered output tuple. | Boolean circuit DAG in topological order. | The fixture describes one NAND truth table but does not model a full direct-wire word. | Report-stated: `VerifyDW`, parser/canonical encoder. | Parser, size, or equivalence checks refer to different circuit objects. | Present only in report; formal source evidence required. |
| Carrier | Report "Base direct-wire framework" | A tuple `L=(partial L, iota L, prof L)` carrying boundary, interface/output, and profile records for constants, slots, origins, kernels, obligations, direction, saturation, budget, and charge. | Typed interface/context for open circuits. | Schematic only: boundary `(a,b)`, output `(o)`, empty obligations. | Report-stated: Package E and framework-match checks. `sourceRef`: `pcc-local-packages0.mjs`. | Replacement compares functions over incompatible interfaces or loses obligation/profile data. | Present only in report for exact fields; formal source evidence required; independent mathematical review required. |
| Locked NAND | Report "Locked NAND SAT embedding"; Appendix A | A locked multi-output NAND direct-wire construction for SAT embedding with disjoint carrier families, macro locks, trace/occurrence slots, macro truth tables, and a threshold theorem. | Restricted circuit encoding or gadget reduction. | The passing fixture uses `gate: "NAND"`, truth table `1110`, one lock input, disjoint slots, and distinct macro outputs. | Local fixture: `checkLockedNand` checks only toy truth table/lock/disjointness flags. `sourceRef`: `pcc-gpack0.mjs`, `pcc-global-proof-dag0.mjs`. `docsRef`: `review/locked_nand_threshold_hostile_review_round1.md`. | Wrong macro truth table, non-disjoint slots, invalid threshold, or construction uses a different size/minimum convention. | Defined in pinned source; independent source audit required; independent mathematical review required. |
| Residual slack | Report "Central scale correction" | `Lambda(C)=size(C)-mu(C)`, where `mu(C)` is the minimum equivalent circuit size in the chosen framework. | Optimality gap or size-to-optimum gap. | Passing fixture: circuit size `6`, certificate minimum `2`, residual slack `4`. | Local fixture: `checkResidualSlack` checks `size - minimumSize` and a stated bound. `sourceRef`: `pcc-local-packages0.mjs`, `pcc-global-proof-dag0.mjs`. | Slack arithmetic mismatch, incompatible minimum definition, or hidden exact minimization used to compute `mu`. | Present only in report for exact `mu` variants; formal source evidence required; independent mathematical review required. |
| Local gain | Report "Central scale correction", Package E/RW sections | For compatible support `U`, the historical report defines `g_C(U)=size(U)-mu*(F_{C,U})` and uses a checked replacement to decrease residual slack. | Local improvement certificate. | No local concrete example beyond residual-slack arithmetic. | Not executable from this checkout. `sourceRef`: `pcc-local-packages0.mjs`. | A claimed gain is not semantics-preserving, not size-decreasing under the same measure, or uses `mu*` as an executable oracle. | Present only in report for exact semantics; formal source evidence required; independent mathematical review required. |
| ZeroSlack | Report "Oracle, ZeroSlack, and residual-band minimization" | A terminal oracle output whose certificate is claimed to prove `Lambda(C)=0`; the report lists ledger, sidecar, selector, blocker, and contradiction obligations. | Terminal case of descent/optimality proof. | [fail-invalid-zeroslack.json](../examples/minimal/fail-invalid-zeroslack.json) has slack `0` but leaves closure open. | Local fixture: `checkZeroSlack` checks only that slack `0` has `status: "closed"` and no positive residual witness. `sourceRef`: `pcc-local-packages0.mjs`. `docsRef`: `review/residual_band_zeroslack_hostile_review_round1.md`. | Zero residual slack is accepted with an open positive witness, missing no-lower ledger, or incomplete contradiction. | Generated artefact required; defined in pinned source; independent source audit required; independent mathematical review required. |
| SAT-to-minimization route | [docs/proof_pipeline.md](proof_pipeline.md), report "Locked NAND SAT embedding" | SAT is mapped to a locked NAND word `W_phi^NAND`; the report claims SAT is decided by comparing an exact minimum size with baseline `B_phi^NAND`. | Karp reduction plus exact optimization decision rule. | No theorem-level local instance generator. | Not executable from this checkout. `sourceRef`: `pcc-gpack0.mjs`, `pcc-final-integration-concrete-materialized0.mjs`. `docsRef`: `REVIEWER_MAP.md`. | Construction is not polynomial, does not preserve satisfiability, or relies on exact minimization inside the reduction. | Defined in pinned source; independent source audit required; independent mathematical review required. |
| FinalMatch / framework match | Report "Locked NAND and final integration artefacts" | Artefact claimed to verify that Package O and Package G use the same syntax, output convention, size/charge convention, carrier constants, minimum notion, and residual slack definition. | Interface compatibility proof. | Schematic only: both packages use NAND gate count and `Lambda=size-minimum`. | Not executable from this checkout. `sourceRef`: `pcc-final-integration-concrete-materialized0.mjs`, `pcc-global-proof-dag0.mjs`. | Minimizer and SAT embedding speak different size, output, carrier, or minimum languages. | Defined in pinned source; independent source audit required; generated artefact required; independent mathematical review required. |

## Proof-System And Checker Terms

| Term | Canonical source in this checkout | Definition for review | Standard concept | Minimal example | Enforcing code path or audit target | Failure mode to test | Uncertainty status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Certificate | Report multiple sections; fixture checker comments | Finite evidence whose fields justify a checker conclusion. In local fixtures, the certificate stores minimum size, residual slack, parser digest, and ZeroSlack status. | Certificate, proof witness, or evidence record. | `certificate` object in the passing fixture. | Local fixture: residual slack, ZeroSlack, and parser-link checks. Not executable from this checkout. `sourceRef`: `pcc-local-packages0.mjs`, `pcc-gpack0.mjs`, `pcc-global-proof-dag0.mjs`. `artifactRef`: generated proof-report bundle. | Checker accepts assertion-like fields without checking evidence. | Generated artefact required; defined in pinned source; independent source audit required. |
| Witness | Report multiple sections; fixtures use `positiveResidualWitness` | A finite object establishing a local claim or failure, such as positive residual presence, a full-mode lift, or a Hall/localization witness. | Mathematical witness. | `zeroSlack.positiveResidualWitness` in fixtures. | Local fixture: only ZeroSlack boolean witness field is checked. Not executable from this checkout. `sourceRef`: `pcc-local-packages0.mjs`. | Missing or malformed witness is treated as a discharged proof obligation. | Generated artefact required; defined in pinned source; independent source audit required; independent mathematical review required. |
| Invariant | Report package ledgers; fixture checker comments | A property preserved by proof steps or required by a checker ledger. | Inductive invariant or ledger condition. | Fixture invariants include import acyclicity, no duplicate row keys, no open obligations, hash match. | Local fixture checker enforces a small set of invariants. `sourceRef`: `pcc-check-pcc-pack-exp0.mjs`, `pcc-global-proof-dag0.mjs`. | An invariant is assumed in downstream proof without being checked or preserved. | Confirmed in public checkout for toy fixtures; theorem-level invariants are defined in pinned source and require independent source audit. |
| Full mode | Report mode sections; [fail-mode-firewall-violation.json](../examples/minimal/fail-mode-firewall-violation.json) | Semantics retaining full carrier/profile/obligation information for constructive replacement use. | Concrete semantics. | Fixture requires `fullLiftRequired: true` and `openObligations: 0`. | Local fixture: `checkModeFirewall` checks only two fields and one forbidden use flag. `sourceRef`: `pcc-global-proof-dag0.mjs`, `pcc-local-packages0.mjs`. | Constructive replacement uses data valid only after quotient/projection. | Defined in pinned source; independent source audit required; independent mathematical review required. |
| Quotient mode | Report mode sections | Projected semantics that can support comparison but cannot by itself authorize full constructive replacement. | Abstract or quotient semantics. | Failing fixture sets `quotientEqualityUsedAsFullReplacement: true`. | Local fixture: `checkModeFirewall`. `sourceRef`: `pcc-global-proof-dag0.mjs`, `pcc-local-packages0.mjs`. | Quotient equality is consumed as full equality without a checked full lift. | Defined in pinned source; independent source audit required; independent mathematical review required. |
| Mode firewall | Report "Mode firewall" language; fixture checker | Rule that quotient equality is not a full replacement proof unless a full lift is checked and obligations are discharged. | Type discipline, abstraction boundary, information-flow control. | [fail-mode-firewall-violation.json](../examples/minimal/fail-mode-firewall-violation.json). | Local fixture: rejects quotient-as-full and nonzero open obligations. `sourceRef`: `pcc-global-proof-dag0.mjs`, `pcc-local-packages0.mjs`; key names include `Mode.Firewall`, `ModeLedger`, `quotientNotReplacement`, and `constructiveFirewall`. | Invalid quotient-to-full transfer, lost-bit obligation not discharged, or finite-kernel obligation ignored. | Confirmed in public checkout for toy fixtures; defined in pinned source for theorem-level use; independent source audit required. |
| Obligation | Report Package E; interface dictionary records | A ledger entry created by some rewrites and discharged by allowed evidence before constructive use. | Proof obligation. | Fixture requires `openObligations: 0` for full-mode use. | Local fixture: `checkModeFirewall` checks only count zero. `sourceRef`: `pcc-local-packages0.mjs`. | Open obligations are ignored, flattened incorrectly, or discharged with wrong evidence. | Defined in pinned source; independent source audit required; independent mathematical review required. |
| No-hidden minimization | Report "No-hidden-minimization scan" and "Checker hardening"; fixture checker | Executable artefacts must not call exact-minimization primitives or aliases in executable position after macro/alias/template/import expansion. | Non-oracle syntactic discipline. | [fail-hidden-minimization-attempt.json](../examples/minimal/fail-hidden-minimization-attempt.json) calls `minimumEquivalent(row)`. | Local fixture: regex scan over toy executable rows. `sourceRef`: `pcc-gpack0.mjs`, `pcc-local-packages0.mjs`, `pcc-global-proof-dag0.mjs`, `pcc-check-pcc-pack-exp0.mjs`; key names include `GPACK_FORBIDDEN_EXEC_SYMBOLS0`, `LOCAL_PACKAGE_FORBIDDEN_EXEC_SYMBOLS0`, `GLOBAL_DAG_FORBIDDEN_EXEC_SYMBOLS0`, `validateNoHiddenExecutableMin0`, and `CheckNoHiddenMin0`. | Hidden call to `mu`, `argmin`, `minimumEquivalent`, `exactMinSearch`, or alias survives expansion. | Confirmed in public checkout for toy fixtures; defined in pinned source for theorem-level use; independent source audit required. |
| Row key | Report "Normal forms and row keys" | Canonical row identity excluding the selected route; used to detect duplicate rows with conflicting classifications. | Canonical cache key or proof-table key. | Passing fixture has a toy `rowKey`; duplicate checking is represented by `noDuplicateRowKeys: true`. | Local fixture trusts a boolean duplicate-row ledger. `sourceRef`: `pcc-check-pcc-pack-exp0.mjs`; row/package checker files are mapped by `docsRef`: `REVIEWER_MAP.md`. | Selected route affects identity, duplicate conflicts are missed, or hash lookup replaces full-key comparison. | Defined in pinned source; independent source audit required. |
| Route | Report package sections | A named proof/checking outcome selected by deterministic priority for a row or branch. | Case split branch or rule application. | Not modeled by local fixtures except as named failure reasons. | Not executable from this checkout. `sourceRef`: `pcc-local-packages0.mjs`. | Lower-priority route hides a higher-priority gain/failure, or route priority is nondeterministic. | Defined in pinned source; independent source audit required; independent mathematical review may still be required. |
| PCC-K | Report "PCC-K and PCC-K Sigma" | A proof-kernel implementation with typed terms, formulas, judgments, primitive rules, side conditions, and proof-DAG checking. | Small proof kernel. | No local executable example. | Not executable from this checkout. Kernel/global proof-DAG files are named by `docsRef`: `REVIEWER_MAP.md`; source inspection is required. | Opaque proof blob, unsound primitive rule, untyped node, or circular proof dependency. | Present only in report and release docs here; formal source evidence required; independent source audit required. |
| Sigma registry | Report "PCC-K and PCC-K Sigma" | Finite schematic theorem instances used by the proof kernel and reflection system. | Schema/theorem registry. | No local executable example. | Not executable from this checkout. Sigma/reflection files are named by `docsRef`: `REVIEWER_MAP.md`; source inspection is required. | Overbroad schema admits invalid row proof or reflected theorem. | Present only in report and release docs here; formal source evidence required; independent source audit required. |
| Global proof DAG | Report "Global proof DAG" | Acyclic graph of kernel, Sigma, row, package, reflection, bounds, no-min, mode, import, and final theorem nodes. | Typed proof dependency DAG. | Local fixture only has `importGraphAcyclic: true`. | Local fixture trusts the acyclicity flag. `sourceRef`: `pcc-global-proof-dag0.mjs`. | Cycle, unchecked import, untyped proof reference, or final theorem dependency mismatch. | Defined in pinned source; independent source audit required; proof-ref soundness remains priority review work. |

## Identity, Hashing, And Release Terms

| Term | Canonical source in this checkout | Definition for review | Standard concept | Minimal example | Enforcing code path or audit target | Failure mode to test | Uncertainty status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Seal / release seal | [downloads/release-seal.json](../downloads/release-seal.json), [tools/verify-release-seal.mjs](../tools/verify-release-seal.mjs), report final section | SHA-256 manifest for named public files in this website checkout. It verifies artefact identity only, not theorem correctness. | Checksum manifest or release attestation. | Fixture `seal.payloadText` plus SHA-256 digest; public manifest lists report PDF/TeX files and the source/checker release reference. | Local: `verify-release-seal.mjs` checks byte count, SHA-256, and `SHA256SUMS` agreement. `artifactRef`: `proof-artifacts/final-pnp-proof-report-hardened-7072f8d/release-seal.json`. | Digest mismatch, byte-count mismatch, manifest/ledger disagreement, or wording implies theorem validation. | Confirmed in public checkout for public files; generated artefact required for source/checker bundle identity. Hashes verify artefact identity only, not theorem correctness. |
| Hash-as-index discipline | Report "Concrete Codec_0 and Digest_0" | Digests may index or label objects, but digest equality is not object equality; lookup must be followed by full-key or canonical-byte comparison. Hashes verify artefact identity only, not theorem correctness. | Content-addressed storage discipline. | [fail-hash-mismatch.json](../examples/minimal/fail-hash-mismatch.json). | Local fixture: `checkSeal` recomputes one payload digest. `sourceRef`: `pcc-core.mjs`, `pcc-check-pcc-pack-exp0.mjs`. Release-audit surfaces are named in `docsRef`: `REPRODUCE.md`. | A checker accepts semantic equality because digest labels match without comparing underlying canonical objects. | Confirmed in public checkout for toy fixture identity; defined in pinned source for theorem-level use; independent source audit required. Hashes verify byte identity only. |
| Canonical bytes | Report "Concrete Codec_0 and Digest_0" | Unique serialization for accepted typed objects; every top-level parse consumes all bytes, with deterministic normal-form serialization. | Canonical serialization. | Fixture `parsedSourceSha256` links `sourceText` to a certificate digest, but this is not the real codec. | Local fixture: `checkCertificateParserLink` checks one text digest. `sourceRef`: `pcc-core.mjs`. Codec targets are named by `docsRef`: `REPRODUCE.md`, `REVIEWER_MAP.md`. | Multiple encodings for one object, one byte string with multiple parses, or accepted trailing bytes. | Defined in pinned source; independent source audit required. Digest matches verify bytes only. |
| Acceptance replay | Report "Final acceptance run and deterministic generator" | Re-check of the acceptance run comparing canonical bytes and accepted records; the report says replay compares canonical bytes, not digest equality alone. | Deterministic replay log. | Not present locally beyond fixture command output. | Not executable from this checkout. `sourceRef`: `pcc-runall0.mjs`, `pcc-final-acceptance-replay0.mjs`. Replay and final-run artefacts are named by `docsRef`: `REPRODUCE.md`. | Generator drift, stale artefacts, or digest-only drift is accepted. | Generated artefact required; defined in pinned source; independent source audit required. A replay pass does not establish theorem correctness. |
| Release gate | Report final proof-report sections | Final check tying accepted final certificate, release audit, final replay, and public theorem fields. | Release certification gate. | `npm run verify:seal` is not the release gate. | Local only has file-identity seal verification. `sourceRef`: `pcc-final-proof-report0.mjs`, `pcc-runall0.mjs`, `pcc-final-pnp-release-gate0.mjs`. Release-gate artefacts are named in `docsRef`: `REPRODUCE.md` and pinned under `artifactRef`. | Final report accepts with stale certificate, missing replay, or unmatched public theorem field. | Generated artefact required; defined in pinned source; independent source audit required. It does not replace mathematical acceptance. |

## Terms Deliberately Not Fully Defined Here

The report contains many package-internal names such as Terminal MuBridge, SaturatePositive, BCELReady, BN2-BN6, Packet, Realizer/HB, HResolve, BUD, PkgC, charge ledgers, selector families, and blocker graphs. This checkout contains prose descriptions of those objects but not enough source/checker artefacts to verify exact schemas, proof rules, or enforcement paths. Treat their detailed definitions as formal source-evidence and generated-artefact targets under the pinned `sourceRef`, `docsRef`, and `artifactRef` model. If those pinned materials do not resolve a term, mark it `Missing or ambiguous; author clarification requested`; the clarification is not verification evidence.
