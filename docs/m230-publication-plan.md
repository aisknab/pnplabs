# M230 and M231 major publication integration

## Current batch decision and source

Publish the complete all-input Cook-Levin formula builder (M230) and concrete
CNF-SAT NP-completeness (M231) together from the verified M231 core merge:
`e4af115a66cd5a6715a9363abea7a2008238d401`, tree
`28bf3e0bb8f4afccf308859c5f42f9c27b14edbb`.
This batch is warranted by two fixed checkpoint transitions, not the number of
new publication rows. The latest published M229 estimate was 35%; the M230
builder earned three points and M231 concrete NP-hardness earned two points.

Current risk-weighted proof completion estimate: 40%, uncertainty 20% to 40%.
Formal artefact coverage: 207 of 209 current scoped publication rows earned.
Global proof gates closed: 0 of 5. Project-specific axioms remaining: 0.
Eligible root `PNP.Main.p_eq_np`: absent. Publication gate: false.
NP-completeness is hardness plus NP membership, not a polynomial-time SAT
decision algorithm. Unconditional residual minimisation, ZeroSlack, complete
polynomial PCCMin bounds, deterministic SAT and the eligible root remain open.

The core feature passed all 1,400 tests. All normal PR and exact-merge workflows
must pass before the site pin changes. Independent exact-merge verification
checks the actual commit and tree, clean state and sealed canonical bytes;
unchanged proof compilation and hostile-suite evidence are reused. PNPLabs
must not rebuild Lean, regenerate the core report or repeat the PNP test suite.

The staged site passed 104 focused tests: 98 browser/publication contracts and
six new M231 contracts. A subsequent README-only correction reran its six
affected contracts and reused the 98 unchanged browser tests. Final source sync,
complete site/publication validation, normal PR and post-merge checks, exact-site
reproduction, deployment and independent production checks remain release gates.
Historical M230 preparation below is retained as an audit trail, not the current
source coordinate or proof-status snapshot. Immutable M230 report bytes remain
unchanged; the current corrected report comes from the exact M231 core merge.

## Historical M230 preparation: decision and source

Publish one major PNPLabs update for the verified complete all-input Cook-Levin
formula builder. This closes the fixed checkpoint
`reductions-complete-cook-levin-builder`, not a global proof gate.
Core source: `a5e6b44e1345b5b1ca124391a5e57f02f95742dd`.
Core tree: `48938071581c3242c612573da1f0eb2f2e3f967e`.
The feature, tested PR merge and actual merge have identical trees; the independent
core reproduction passed 1,391 tests. All four required post-merge workflows passed for this exact core merge.

## Producer and consumer matrix

| Changed authority | Consumers updated together | Cheapest rejecting check |
| --- | --- | --- |
| Four complete-builder status flags, six theorem pins and exact scope/non-claim | M230 shared contract; synchronizer; seal and cross-repository validators; browser evidence registry; current claims | M230 positive and hostile status, inventory, map and release mutations |
| Canonical fixed checkpoint and history: 35 to 38 points | Progress validator, mirrored ledger, primary cards, FAQ, graphic, updates/feed, current summaries | Progress arithmetic, evidence and generated-surface checks |
| Core merge/tree and generated artifact identities | Release manifest, seal, source targets, browser hashes, downloads, index, workflow/source tests | Byte identity, source binding and seal checks |
| Current latest-milestone selection | Browser and cross-repository mutation field selection, update entry, current bottom line and reviewer docs | Latest-milestone mutation family and rendered-copy tests |

The status has four general builder flags, not a fabricated per-module
`AxiomAuditPassed` flag. Audit evidence is the six exact compiled theorem closures
and the earned publication row. Mutation tests must exercise that actual interface.
Execution pins allow only `propext` and `Quot.sound`; the two reduction-facing
semantic pins also retain the already allowed standard `Classical.choice`.
Do not widen the project-axiom boundary.

## Verification ownership and sequence

1. Prepare source and every affected expectation together. Preserve historical
   M229 and earlier entries, including their original score and source coordinates.
2. After required exact-core-merge checks pass, synchronize exact canonical bytes.
   Do not compile Lean, rerun core tests, rebuild the PDF or install dependencies.
3. Run syntax and focused M230/browser/progress positive and negative contracts
   first; only then run the complete site/publication suite and source-bound audit.
4. Inspect all active site surfaces, links, metadata, downloads and browser-rendered
   fail-closed status. Do not display PDF page totals as public progress copy.
5. Review the complete public diff and privacy boundary, open a draft PR, wait for
   normal checks, merge manually with an unchanged-head guard, then verify the exact
   site merge. Reuse expensive evidence only for identical trees, inputs, toolchain
   and boundary; retain required PR/post-merge, clean-object, staging and production gates.
6. Deploy only the verified site merge and independently verify production.
   Record final results in the PR and release handoff, with the checked commit and tree.

## Claim and progress boundary

Risk-weighted proof completion estimate: 38%, uncertainty 20% to 40%.
Formal artefact coverage: 206 of 208 current scoped publication rows earned.
Global proof gates closed: 0 of 5. Project-specific axioms remaining: 0.
Eligible root `PNP.Main.p_eq_np`: absent. Publication gate: false.

The builder constructs a CNF formula in polynomial time; it does not decide
satisfiability. Named concrete NP-hardness/NP-completeness transport, unconditional
residual minimisation and ZeroSlack, complete polynomial PCCMin bounds, deterministic
SAT and the eligible root remain open.

## Verified publication preflight

- Focused M230 status, inventory, map and release contracts passed, including hostile mutations.
- Current summaries describe the earned builder checkpoint and separate it from SAT decision and the five still-open global gates. Current inventory counts derive from the compiled payload.
- The static status preflight checks all 394 required fragments and all 208 milestone records. Homepage and status fallbacks expose the four actual builder fields; current-copy mutations reject stale fields, bottom lines, score transitions and module counts.
- Chrome verified the homepage, status, FAQ, paper, architecture and updates at desktop and mobile widths: 12 views, no horizontal overflow, correct progress and coverage, and authoritative inventory-backed state where present.
- The earlier unfinished broad audit was stopped when the editorial review exposed stale current copy. It is not release evidence. The final complete site audit, PR checks, exact-merge verification and deployment checks remain required.
- No immutable historical release artefacts or core proof bytes were changed by this publication cleanup.

## Final expectation-family review

The complete site run exposed stale current-state expectations in older M217
through M228 tests. Their local theorem boundaries and historical snapshots stay
unchanged; current builder flags now follow the earned complete-builder row.
The homepage test uses the actual four-field M230 interface, the source-byte
test honors the explicit source checkout and tracked canonical status paths,
and generic progress assertions calculate earned points from fixed checkpoints.
Independent M230 positive and hostile contracts still enforce the six compiled
pins, exact source identity, 35-to-38 transition and unchanged global gates.

Run these affected test families before the next complete matrix. The earlier
414-pass, 16-failure matrix is not a green release result. No core proof rerun
is needed for these website-test corrections.

The read-only publication workflow now permits 75 minutes instead of 45. Recent
successful M229 runs took about 34 minutes, and the M230 inventory is larger;
this provides cleanup and audit headroom without removing checks or widening
permissions. No workflow shell block changes in this correction.

## Publication hold and batch amendment, 2026-09-11

Do not deploy this standalone M230 candidate. Inspection of the exact core PDF
found an obsolete cover statement that the complete raw polynomial-time
Cook-Levin builder is missing, despite the correct completed M230 body section.
M231 has now kernel checked concrete CNFSAT NP-completeness and will correct
the current report template during its core integration. Publish M230 and M231
together only after the exact M231 core merge and its required verification.

Preserve the prepared site changes and the coherent M230 source pin until then.
Do not rewrite the historical M230 report or bind the website to an unmerged
branch. The corrected 72-test focused family and four workflow checks passed;
the following broad site audit was intentionally stopped for this report
correction and is not complete release evidence. The final batch needs its own
source-bound site audit and deployment verification, but no duplicate Lean build.

## M231 batch producer and expectation review

The M231 core feature has passed 1,400 tests and its explicit root, two-theorem
axiom audit, four unbounded regressions, compiled inventory and corrected report
checks. Its PR and exact-merge gates remain required before changing this site's
source pin. This preparatory review does not authorize an unmerged source pin.

| M231 authority | Consumers to reconcile together | First rejecting checks |
| --- | --- | --- |
| Five concrete CNF-SAT NP-completeness fields and two exact theorem pins | Shared M231 contract, synchronizer, seal/cross-repository validators, browser registry and fallback, index and latest-milestone field selection | Missing/duplicate/weakened/assumed theorem and false/absent/wrong-type status mutations |
| Fixed `reductions-concrete-np-hardness` checkpoint, 38 to 40 points | Canonical mirror, validator, primary cards, FAQ, graphic, M231 update and feed | Exact two-point history transition, fixed weights and no root-transport double credit |
| Verified merge/tree, report, status, inventory and ledger bytes | Release manifest, seal, source targets, workflow pins, browser hashes and download aliases | Source/object and byte-identity checks, then generated-output check mode |
| NP-completeness is now earned, SAT decision remains open | Current prose and old tests of current state, including earlier conditional-milestone tests | Current-copy and browser mutation families before the broad suite |

Preserve the M230 history entry, original coordinates and its six theorem pins.
Current M230 tests must not require the entire site to stay pinned to M230.
Retain fail-closed fallback defaults and independent hostile-test invariants.
Do not award a final complexity-transport point or close a global gate: M231 is
NP-hardness plus NP membership, not a polynomial-time SAT decision algorithm.
The intended batch has risk-weighted proof estimate 40%, uncertainty 20% to 40%,
formal artefact coverage 207 of 209, no project axioms, an absent eligible root,
and all five global gates open. Generate those values from the exact verified
core merge; do not change historical release artefacts or rebuild Lean in PNPLabs.
