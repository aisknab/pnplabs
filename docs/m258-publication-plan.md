# M258 major computational-support publication batch

## Publication decision and release boundary

Publish one PNPLabs batch after the M253-M258 core capability release is fully
earned. The trigger is complete source-derived proper zero/unary support
discovery joined to actual full-field-preserving replacement, not the number
of intervening rows. Include all earned core work since the published M231
snapshot, M232 through M258, in this one website cycle.

The previously published site source is M231 core merge
e4af115a66cd5a6715a9363abea7a2008238d401, tree
28bf3e0bb8f4afccf308859c5f42f9c27b14edbb.
Its coherent 207/209 coverage, 40% estimate and 20% to 40% uncertainty remain
together in production until this exact M258 publication batch is released.

Preparation reviewed core PR 451 at
ce49f5a8212e107399cd178cdc575d9bef312673, tree
78648f5978d16ff5055c5b493e5bb97ff4cd7cac.
The six mathematical milestone trees retain their individual verification
evidence. Two subsequent workflow-only maintenance commits preserve the Lean
commands and proof bytes. This feature commit is not a publication coordinate.

Core PR 451 is earned and merged as
6296c6e1130fbae674548ccbf949f6e608b996c7, tree
78648f5978d16ff5055c5b493e5bb97ff4cd7cac. All four normal PR checks and
all four exact post-merge checks passed. Independent clean exact-merge
identity, source binding, publication/progress seals, archive references and
CLI verification passed with a clean worktree. The already verified core
suite covers 259 files and 1,696 tests; PNPLabs reuses that proof evidence.
This exact M258 merge is the publication cut. M259 and M260 are separate
mathematical milestones and do not expand this batch.

The shared contract is derived from that exact merge and covers all 27 new
rows, 540 distinct status fields and 369 distinct reviewed theorem names.
Its browser descriptor and validators are generated from the same module.
The browser checks the complete status and inventory byte digests before
using the shared metadata checks; Node additionally hashes every kernel type.

## Public bottom line and claim boundary

The batch reconstructs a computational path from physical support accounting
through derived normalization and full-field restoration to actual local
replacement. M258 computes a complete family of proper zero/unary supports
from the source and finds a strict gain whenever any support in that class
admits a smaller equivalent complete local word.

Define the jargon on first use: a support is a selected set of circuit gates;
its incoming boundary consists of wires entering from outside; full-field
preservation includes the tracked computational observations, not just the
ordinary circuit outputs.

The scoped search is not a global circuit minimizer. It does not cover all
boundary widths or the full manuscript carrier and obligation calculus.
Its physical candidate-count bounds do not prove the complete procedure runs
in polynomial encoded-input time. Unconditional SaturatePositive, BCELReady
and ZeroSlack, exact general PCCMin, deterministic SAT and the eligible root
remain open.

Preserve M230's complete polynomial Cook-Levin builder and M231's concrete
CNF-SAT NP-completeness. M243 connects this concrete hardness to the still
conditional final-report bridge; do not advertise that bridge as the root
theorem or an unconditional P = NP proof.

From the reviewed M258 core ledger, the expected current formal artefact
coverage is 234 of 236 scoped publication rows. Risk-weighted proof completion
remains 40%, uncertainty 20% to 40%, global gates 0 of 5, project-specific
axioms 0, root PNP.Main.p_eq_np absent, publication gate false. Derive the
published values from the exact merged ledger; no checkpoint changes here.

## Producer and expectation matrix

| Authority or changed boundary | Consumers to reconcile together | First rejecting checks |
| --- | --- | --- |
| Exact verified core commit/tree and canonical report, status, inventory, map and progress bytes | Synchronizer pins, source targets, release manifest/seal, index, download aliases, report-integrity hashes and durable workflow assertions | Clean source identity, byte equality and changed-field summary before site generation |
| The 27 pending earned rows, their precise theorem types/axioms, scopes, non-claims and status fields | One shared batch contract, synchronizer, seal validator, cross-repository validator and browser evidence registry | Missing/duplicate/moved/weakened/assumed theorem; absent, false and wrong-type status; changed scope and fingerprint mutations |
| Current latest milestone is proper zero/unary support search | Latest-milestone selection helpers, browser fallback and rendered current flags, release-boundary mutation tests | Entire latest-milestone mutation family, not just one positive example |
| Historical and current metric snapshots from the fixed core ledger | Progress surfaces, each new update/feed entry, graphic, FAQ and current summaries | Fixed checkpoint arithmetic, unchanged score, independent coverage and exact history-coordinate checks |
| General computational capability and remaining global obligations | Homepage bottom line, status ledger, FAQ, paper, architecture, README, reviewer/audit/source documentation | Current-copy and negative overclaim checks, then desktop/mobile browser review |
| Growing generated inventories and unchanged historical evidence | Child-process byte reads, synthetic fixture sizes, exact seals and historical refs | Buffer/fixture preflight from canonical sizes and ref existence; no guessed padding or broad numeric replacement |

The shared batch contract should follow the existing M229-M231 contract
interfaces and preserve earlier contracts. Generate only the new reviewed
descriptors from exact core evidence and keep one validator implementation;
do not paste a separate large validation block into every consumer for each
row. This is a bounded batch integration, not a rewrite of legacy validators.

Review changed existing theorem evidence as well as added declarations,
especially the conditional final-report bridge. Derive source pins, exact
scope and non-claim text, hashes, theorem-name sets, module mappings, status
fields and artifact sizes from the authority, not from expected test output.
Retain independent fixed-checkpoint, semantic and hostile-mutation invariants.

## Update history and surfaces

Append a distinct update for each newly published earned milestone, using its
actual core history coordinate and original metric snapshot. The single batch
release must not invent retrospective score increases or replace previous
entries. Keep original historical source coordinates and immutable release
artifacts byte-for-byte. The 27 first-published batch entries share one actual
publication timestamp and the exact reviewed M258 source commit/tree; each
retains its own original core history coordinate and metrics. Do not invent
separate retrospective publication times. Equal timestamps require the same
commit, tree and publication coordinate, while chronological ordering remains
newest first. Tests cover shared-source ties, conflicting identities, fractional
seconds and the immutable digest of every prior update.

The existing update and progress generators remain authoritative for generated
HTML, feed and graphic output. Reconcile the entire current site surface and
accessible fallback, not just the large progress card.

Do not publish PDF page totals as progress copy or manually duplicate them in
current documentation. Preserve exact core PDF and TeX bytes and check their
machine-readable release metadata. Use no em dashes in website prose.

## Reviewer-document integration

Current source links, latest-result summaries, evidence tables and reproduction
expectations consume the exact publication cut and canonical tracker. Current
reviewer blocks distinguish the zero/unary computational search from global
minimization and total polynomial runtime; the M243 report bridge remains
conditional. The shared marker identifies only current prose, while explicit
historical worksheets, earlier component tables and per-milestone estimates are
preserved byte-for-byte. Focused tests retain the earlier no-obsolete-blocker
and no-duplicated-inventory-count safeguards for the M258 current publication.
The reviewer command sequence reuses the seal check already included in the
site suite instead of prescribing an unchanged second run.

The verification-page checkout command and current terminology links bind to
the same published source rather than an earlier M229 pin. Focused tests derive
those expectations from the latest canonical update. Current computational terms
include definitions, examples, exact enforcing interfaces and rejection limits;
the immutable historical terminology remains unchanged.

The complete static ledger retains every reviewed theorem interface and the
original fixed-model history snapshots generated from the canonical ledger.
Browser rendering preserves those dated annotations without using them in the
current publication gate. The current homepage and status identity panels expose
the exact compiled-inventory digest separately from mathematical claims.

Current fixture adapters resolve nested batch fields through the shared helper
for artifact, public-payload, homepage, browser and cross-repository consumers.
The legacy flat representation remains covered. Browser count decoding compares
actual object values rather than formatting. Current batched reviewer contracts
require source-bound titles, scope, separate metrics, non-claims and canonical
inventory links; changing declaration/audit totals are not duplicated across
every guide. Formal status and the exact reproduction identity table retain
their distinct metadata checks. Historical title checks use canonical ledger
titles while fixed milestone identities and proof-boundary checks remain.

The batched static-ledger contract compares every card's canonical order,
earned state, title, complete scope, complete non-claim and exact reviewed
theorem list. Hostile fixtures reject altered scope, limitations, identity,
state and pins. This replaces historical copy-fragment expectations for the
current batch, including superseded builder-prefix descriptions and retired
audit counters; the legacy flat-release contract is retained. Homepage checks
retain the full current batch fields, separate metrics and open global gates,
without requiring every historical counter on the landing page. Summary cards
may use a concise shared headline and must link to the canonical current update.

Current homepage, bottom-line and reviewer copy must contain the complete
canonical plain-language milestone account, rather than satisfy a keyword ratio
against a much longer technical theorem scope. Concise summary cards retain
independent proper-support, zero/unary, output-preservation, global-minimization
and runtime boundaries and link to the exact current update. The full technical
scope and non-claim remain exact on every static milestone card.

The synthetic cross-repository map projection now retains each canonical title,
as required by the complete batch boundary. Its entire batch is accepted by the
actual publication-map validator at module startup before any costly mutation
fixture is created. The real pinned-source audit passed independently; only the
synthetic baseline omitted that field. Recheck the baseline acceptance first,
then the complete affected mutation family in independent bounded shards. Reuse
unchanged site-unit, browser, generated-byte and proof evidence rather than
repeating those layers for this fixture-only correction.

## Verification ownership and sequence

1. Complete the independent core release boundary before source synchronization.
   Keep this preparation and source-review work separate from current public pins.
2. Fetch the exact verified core merge into a clean named source checkout and
   verify every current and historical ref named by audit_targets.json.
3. Reconcile producer code, shared contract, latest-milestone helpers, current
   copy and their complete test/fixture/workflow expectation families together.
   Generate the canonical mirrors and derived surfaces only after source inputs
   stabilize. Record every structured field delta.
4. Run affected syntax and focused positive/hostile publication, progress,
   browser and current-copy contracts first. Only then run the complete site
   suite and source-bound cross-repository audit with PNP_SOURCE_DIR set.
   Do not install dependencies for dependency-free scripts.
5. Review all current desktop/mobile publication surfaces, source links,
   downloads, visible gates, progress wording, accessibility and overflow.
   Seal the final reviewed public bytes and reproduce the exact candidate.
6. Open one focused draft PR, wait for every normal read-only check, verify
   head identity and reviews, and merge manually. Require every exact-site
   post-merge check and one clean exact-merge reproduction.
7. Deploy the exact verified site merge through the authorized narrow launcher.
   Independently verify production identity, routes, bytes, provenance and
   service health. Send meaningful progress and deployment notifications.
8. Record public release evidence and remove all exact task-created temporary
   checkouts, logs, helper scripts and transport artifacts after their purpose
   is complete.

PNP owns Lean compilation, theorem and axiom checking and core report generation.
PNPLabs reuses that evidence and checks its distinct publication and deployment
boundaries. Do not execute Lean, Lake, Elan or the PNP test suite for the site.
Do not repeat an unchanged expensive green layer for a new merge parent when
the release policy permits exact-object and seal checks instead.
